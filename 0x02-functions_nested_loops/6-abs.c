#include "main.h"
/**
 * _abs - write a function that computes the abs value of an integer
 * @n: the absolute number to be computed
 * Return: the absolute value of the number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
